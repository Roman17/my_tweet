# Erlang version
erlang_version=19.2

# Elixir version
elixir_version=1.3.4

# Always rebuild from scratch on every deploy?
always_rebuild=false

# A command to run right after compiling the app
post_compile="mix run --no-halt"
